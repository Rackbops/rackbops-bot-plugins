{
  "schemaVersion": 1,
  "generatedAt": "2026-09-09T06:00:35.594Z",
  "plugins": [
    {
      "name": "warbandeer",
      "package": "@rackbops/plugin-warbandeer",
      "version": "1.2.0",
      "description": "Warbandeer desktop-app character linking (/link, /unlink, ingest endpoint)",
      "hostApiVersion": 1,
      "intents": [],
      "commands": [
        "link",
        "unlink"
      ],
      "env": [
        {
          "key": "WARBANDEER_INGEST_PORT",
          "format": "^([1-9][0-9]{0,3}|[1-5][0-9]{4}|6[0-4][0-9]{3}|65[0-4][0-9]{2}|655[0-2][0-9]|6553[0-5])$",
          "description": "Port the ingest server binds inside the container; unset = connector off (never published to the host; reachable only through the Cloudflare Tunnel)",
          "required": false,
          "secret": false
        }
      ],
      "adminUrl": "https://cdn.jsdelivr.net/npm/@rackbops/plugin-warbandeer@1.2.0/dist/admin.js",
      "adminApiVersion": 1,
      "releases": [
        {
          "version": "1.2.0",
          "publishedAt": "2026-09-09T00:00:00.000Z",
          "url": "https://github.com/Rackbops/rackbops-bot-plugins/releases/tag/warbandeer-v1.2.0",
          "notes": "### Added\n\n- `dispose()` (`rackbops-discord-bot#184`): the ingest server this plugin's `activate()` opens is\n  now closed on the way out — a `docker stop`, a self-update's retire, `SIGINT` — instead of being\n  left listening until the process is killed out from under it. No behaviour change if the\n  connector was never configured (`WARBANDEER_INGEST_PORT` unset) or never started."
        },
        {
          "version": "1.1.0",
          "publishedAt": "2026-09-06T00:00:00.000Z",
          "url": "https://github.com/Rackbops/rackbops-bot-plugins/releases/tag/warbandeer-v1.1.0",
          "notes": "### Added\n\n- An admin-panel tab, via the plugin admin-UI contract\n  (`rackbops-discord-bot#123`): shows whether the ingest connector is running and\n  lets an admin set (or clear) `WARBANDEER_INGEST_PORT` — saved through the panel's\n  guarded env-set, which recreates the bot to apply it. No character/link data is\n  shown."
        },
        {
          "version": "1.0.0",
          "publishedAt": "2026-09-04T00:00:00.000Z",
          "url": "https://github.com/Rackbops/rackbops-bot-plugins/releases/tag/warbandeer-v1.0.0",
          "notes": "### Added\n\n- Warbandeer desktop-app character linking, ported from the bot's baked-in connector\n  (`rackbops-discord-bot#3`, PR #92) as the first published plugin: the `/link` and `/unlink`\n  slash commands and the HTTP ingest endpoint (`POST /link`, `POST /characters`), gated behind\n  `WARBANDEER_INGEST_PORT` (unset = connector off). Behaviour is identical to the baked-in\n  connector; see the bot's `docs/adr/0001`–`0003` for the transport, auth, and storage design."
        }
      ]
    },
    {
      "name": "wow",
      "package": "@rackbops/plugin-wow",
      "version": "1.0.1",
      "description": "World of Warcraft features: Darkmoon Faire schedule, resets, realm up/down watch, and /transmog import strings (/dmf, /reset, /status, /transmog)",
      "hostApiVersion": 1,
      "intents": [],
      "commands": [
        "dmf",
        "reset",
        "status",
        "transmog"
      ],
      "env": [
        {
          "key": "WOW_REGION",
          "format": "^(us|eu)$",
          "description": "Blizzard region: us or eu (default us). Sets the reset times and the Darkmoon Faire timezone default.",
          "required": false,
          "secret": false
        },
        {
          "key": "WOW_REALM",
          "format": "^[a-z0-9àáâãäåæçèéêëìíîïðñòóôõöøùúûüýþÿ-]{1,40}$",
          "description": "Realm slug whose up/down status /status reports and the realm watch announces; blank tracks no realm.",
          "required": false,
          "secret": false
        },
        {
          "key": "DMF_TIMEZONE",
          "format": "^[A-Za-z0-9+_-]+(/[A-Za-z0-9+_-]+){0,2}$",
          "description": "IANA time zone for Darkmoon Faire timing (default from WOW_REGION). Validated as a real zone by the plugin at load.",
          "required": false,
          "secret": false
        },
        {
          "key": "BLIZZARD_CLIENT_ID",
          "format": "^\\S+$",
          "description": "Blizzard API client id (needed for /status, /transmog, and the realm watch).",
          "required": false,
          "secret": true
        },
        {
          "key": "BLIZZARD_CLIENT_SECRET",
          "format": "^\\S+$",
          "description": "Blizzard API client secret.",
          "required": false,
          "secret": true
        }
      ],
      "adminUrl": "https://cdn.jsdelivr.net/npm/@rackbops/plugin-wow@1.0.1/dist/admin.js",
      "adminApiVersion": 1,
      "releases": [
        {
          "version": "1.0.1",
          "publishedAt": "2026-09-09T00:00:00.000Z",
          "url": "https://github.com/Rackbops/rackbops-bot-plugins/releases/tag/wow-v1.0.1",
          "notes": "### Fixed\n\n- `rackbops-discord-bot#55` item 1: `/transmog` with a non-Latin-script realm name (e.g. a\n  Cyrillic EU realm typed as shown in-game) used to slug to an empty string, reach Blizzard's\n  API anyway, and reply blaming the *character* (\"No character **Name** on **** (EU)\") rather\n  than naming the actual problem. An empty or hyphen-only slug is now rejected before any fetch,\n  with a reply naming the realm as typed and asking for the English realm name. No name→slug map\n  for non-Latin realms in this release — the plugin's own embedded realm list carries only\n  English names, so there's nothing to resolve a Cyrillic name *from* yet. Latin realm names with\n  hyphens or apostrophes (`rackbops-discord-bot#32`) are unaffected."
        },
        {
          "version": "1.0.0",
          "publishedAt": "2026-09-06T00:00:00.000Z",
          "url": "https://github.com/Rackbops/rackbops-bot-plugins/releases/tag/wow-v1.0.0",
          "notes": "### Added\n\n- World of Warcraft features, ported from the bot's baked-in code\n  (`rackbops-discord-bot#107`): the `/dmf`, `/reset`, `/status`, and `/transmog`\n  slash commands; the Darkmoon Faire, weekly-reset, and realm up/down\n  announcements; and a realm-chooser admin-panel tab (via the plugin admin-UI\n  contract, `rackbops-discord-bot#123`) with a region-filtered realm dropdown and\n  a Darkmoon Faire timezone picker. Behaviour is identical to the baked-in code;\n  the dedup state lives in the plugin's own `data/wow.json`, seeded once from the\n  bot's `state.json` so an existing instance never re-announces after the move."
        }
      ]
    }
  ]
}
